---
description: Gemini-Flux-Chat 项目主规则 - 多模态AI聊天应用
globs:
  - "**/*"
---

# Gemini-Flux-Chat 项目规则

## 项目概述

这是一个多模态AI聊天应用，支持文本对话、图像生成/编辑、视频生成、音频生成、PDF处理、深度研究和多代理工作流等功能。

## 技术栈

### 前端 (frontend/)
- **框架**: React 19 + TypeScript + Vite 6
- **样式**: Tailwind CSS 3
- **路由**: React Router 7
- **状态管理**: React Context + Custom Hooks
- **存储**: IndexedDB (通过 services/db.ts)
- **流编辑**: ReactFlow / XYFlow
- **Markdown**: React Markdown + React Syntax Highlighter

### 后端 (backend/)
- **框架**: FastAPI + Uvicorn
- **ORM**: SQLAlchemy 2 (异步)
- **数据库**: PostgreSQL / SQLite
- **认证**: JWT (python-jose)
- **AI SDK**: google-genai, openai, dashscope
- **缓存**: Redis

## 代码规范

### 通用规范
1. 使用 TypeScript/Python 类型注解
2. 遵循函数式编程风格
3. 保持单一职责原则
4. 错误处理要完整
5. 添加必要的日志记录

### 前端规范
1. 组件使用函数式组件 + Hooks
2. 使用 Tailwind CSS 原子类，避免内联样式
3. 自定义 Hook 放在 `hooks/` 目录
4. 业务服务放在 `services/` 目录
5. 类型定义放在 `types/` 目录
6. 处理器类放在 `hooks/handlers/` 目录

### 后端规范
1. 路由放在 `routers/` 目录，按功能分类
2. 业务逻辑放在 `services/` 目录
3. 数据模型放在 `models/` 目录
4. 配置放在 `core/` 目录
5. 使用依赖注入获取数据库会话

## 目录结构约定

### 前端核心目录
```
frontend/
├── components/          # React 组件
│   ├── auth/           # 认证组件
│   ├── layout/         # 布局组件
│   ├── chat/           # 聊天组件
│   ├── views/          # 视图组件（各模式）
│   ├── modals/         # 模态框
│   ├── message/        # 消息组件
│   ├── multiagent/     # 多代理工作流
│   └── common/         # 通用组件
├── hooks/              # 自定义 Hooks
│   └── handlers/       # 请求处理器
├── services/           # 业务服务
├── types/              # 类型定义
├── utils/              # 工具函数
├── controls/           # 控制UI组件
│   └── modes/          # 各模式控制
└── contexts/           # React Context
```

### 后端核心目录
```
backend/app/
├── core/               # 核心配置
├── models/             # 数据模型
├── middleware/         # 中间件
├── routers/            # API 路由
│   ├── auth/           # 认证路由
│   ├── core/           # 核心路由
│   ├── system/         # 系统路由
│   ├── tools/          # 工具路由
│   ├── ai/             # AI 功能路由
│   ├── models/         # 模型管理路由
│   ├── user/           # 用户路由
│   └── storage/        # 存储路由
└── services/           # 业务服务
    ├── common/         # 通用服务
    ├── gemini/         # Google Gemini
    ├── openai/         # OpenAI
    ├── tongyi/         # 通义千问
    └── ollama/         # Ollama
```

## 应用模式系统

项目支持 13 种应用模式 (AppMode)：
- `chat` - 文本聊天
- `image-gen` - 文生图
- `image-chat-edit` - 聊天式图像编辑
- `image-mask-edit` - 遮罩编辑
- `image-inpainting` - 图像修复
- `image-background-edit` - 背景编辑
- `image-recontext` - 图像重新生成
- `image-outpainting` - 图像扩展
- `video-gen` - 视频生成
- `audio-gen` - 音频生成
- `pdf-extract` - PDF 提取
- `virtual-try-on` - 虚拟试衣
- `deep-research` - 深度研究
- `multi-agent` - 多代理工作流

## AI 提供商支持

### 完整实现
- Google Gemini (聊天、图像、研究、多代理)
- OpenAI (聊天、DALL-E、TTS)
- 阿里通义千问 (聊天、图像)
- Ollama (本地模型)

### OpenAI 兼容提供商
- DeepSeek, Moonshot, SiliconFlow, ZhiPu, 豆包, 混元, NVIDIA NIM, OpenRouter

## 常见开发任务

### 添加新的应用模式
1. 在 `types/types.ts` 添加 AppMode 类型
2. 创建 `components/views/XxxView.tsx` 视图组件
3. 创建 `controls/modes/XxxControls.tsx` 控制组件
4. 在 `hooks/handlers/` 添加处理器类
5. 在 `App.tsx` 添加路由配置
6. 后端在 `core/mode_method_mapper.py` 添加映射

### 添加新的 AI 提供商
1. 后端在 `services/` 创建提供商目录
2. 实现 `BaseProviderService` 接口
3. 在 `provider_config.py` 添加配置
4. 前端在 `services/providers/` 添加客户端

### 添加新的 React Hook
1. 在 `hooks/` 目录创建 `useXxx.ts`
2. 遵循 React Hooks 命名约定
3. 导出类型定义
4. 在 `hooks/index.ts` 重新导出

## 测试规范

### 前端测试
- 使用 Vitest 框架
- 测试文件放在 `__tests__/` 目录或 `.test.ts(x)` 后缀

### 后端测试
- 使用 pytest + pytest-asyncio
- 测试文件放在 `tests/` 目录

## 环境配置

### 前端 (vite.config.ts)
- 开发服务器: `localhost:21573`
- 后端代理: `localhost:21574`

### 后端 (.env)
- 数据库: `DATABASE_URL`
- Redis: `REDIS_HOST`, `REDIS_PORT`
- JWT: `JWT_SECRET_KEY`, `JWT_ACCESS_TOKEN_EXPIRE_MINUTES`
- AI Keys: `GEMINI_API_KEY`, `OPENAI_API_KEY` 等
