---
alwaysApply: true
---

以下是为AI项目修改任务细化的通用回复规则要求。

---

# AI项目代码修改通用回复规范

## 1. 核心约束原则

为了确保回复的清晰度和规范性，必须严格遵守以下原则：

1.  **语言纯净性（非代码部分）：**
    *   **解说文本：** 所有的分析、解释、步骤说明、总结等非代码内容，必须**完全使用中文**。
    *   **字符隔离：** 在中文解说文本中，如果必须引用代码中的**变量名、函数名、文件名、路径**或**保留字**（均为英文字符），**必须**使用Markdown的**行内代码语法**（即用反引号 \` 包裹，例如 \`main.py\`）进行隔离。
    *   **禁止混排：** 严禁在中文句子中直接出现裸露的英文字母或符号（Markdown语法符号除外）。

2.  **格式标准化：**
    *   **Markdown：** 全文必须始终保持Markdown格式。
    *   **结构化：** 回复必须分步骤、分模块，禁止堆砌大段文字。

## 2. 回复结构定义

一个标准的“项目代码修改”回复，应严格按照以下顺序和结构输出：

### 第一部分：修改概览 (Modification Overview)

*   **目的：** 用中文简要说明本次修改的目标。
*   **内容：**
    *   识别到的问题或需求。
    *   拟定的修改方案简述。
    *   涉及的文件列表。

### 第二部分：文件修改详情 (File Modification Details)

这是回复的核心。对于每一个涉及修改的文件，必须**独立**展示，且遵循以下子结构：

#### 2.1. 文件标识
*   使用三级标题 (`###`) 明确指出当前操作的文件路径。
*   **格式：** `### 修改文件：` + \`文件路径/文件名\`

#### 2.2. 修改说明
*   用中文列出该文件具体的修改点。
*   解释修改的原因（例如：修复逻辑错误、优化算法、增加参数处理）。

#### 2.3. 代码变更展示
必须根据修改的规模，选择以下**两种模式之一**进行展示，并在回复中保持一致：

*   **模式 A：全量替换（推荐用于小文件或重构）**
    *   提供修改后**完整**的文件代码。
    *   代码块上方需标注：`**以下是修改后的完整代码：**`
*   **模式 B：上下文差异（推荐用于大文件）**
    *   仅展示被修改的代码片段及其上下文（上下各保留关键行）。
    *   必须使用注释清晰标记出`<<<< 原代码` 和 `>>>> 修改后代码`，或者直接提供修改后的片段并注明“替换原函数 X”。
    *   代码块上方需标注：`**定位到以下代码段并进行替换：**`

### 第三部分：依赖与配置更新 (Dependencies & Config)

*   如果修改涉及新增库、环境变量变更或配置文件（如 \`requirements.txt\`, \`package.json\`, \`.env\`）的更新，必须单独列出。
*   如果没有变更，此部分可省略。

### 第四部分：验证与测试 (Verification & Testing)

*   **测试方法：** 用中文指导用户如何验证修改是否生效。
*   **测试用例：** 提供具体的测试代码或命令行指令（放在代码块中）。
*   **预期结果：** 用中文描述测试通过时的预期表现。

---

## 3. 详细格式示例

以下是一个符合上述规范的回复模板示例：

```markdown
# 项目代码修改方案

## 1. 修改概览

本次修改旨在解决用户登录模块中的**会话超时未自动跳转**的问题。我们将修改前端路由拦截器以及后端的令牌验证逻辑。

**涉及文件：**
*   `src/router/index.js`
*   `server/middleware/auth.py`

---

## 2. 文件修改详情

### 修改文件：`src/router/index.js`

**修改说明：**
在全局前置守卫中增加了对令牌过期时间的检查逻辑。如果当前时间超过过期时间，将强制重定向到登录页。

**代码变更（上下文替换模式）：**

```javascript
// 定位到 router.beforeEach 方法内部
router.beforeEach((to, from, next) => {
    const token = getToken();
    // >>>> 修改开始：新增过期检查
    if (token) {
        const checkTime = getExpirationTime();
        if (new Date().getTime() > checkTime) {
            removeToken();
            next('/login');
            return;
        }
    }
    // <<<< 修改结束
    
    if (to.path === '/login') {
        next();
    } else {
        // ... 原有逻辑
        next();
    }
});
```

### 修改文件：`server/middleware/auth.py`

**修改说明：**
后端验证中间件现在会在返回 `401` 状态码时，附带具体的错误信息代码，以便前端区分是“无效令牌”还是“过期令牌”。

**代码变更（全量函数模式）：**

```python
def verify_token(request):
    token = request.headers.get('Authorization')
    if not token:
        return {'status': 401, 'code': 'TOKEN_MISSING', 'msg': '未提供令牌'}
    
    try:
        payload = decode_jwt(token)
        return {'status': 200, 'user': payload}
    except ExpiredSignatureError:
        # 修改点：明确返回过期错误码
        return {'status': 401, 'code': 'TOKEN_EXPIRED', 'msg': '令牌已过期'}
    except InvalidTokenError:
        return {'status': 401, 'code': 'TOKEN_INVALID', 'msg': '无效令牌'}
```

---

## 3. 验证与测试

请按照以下步骤验证修改是否生效：

1.  **模拟过期：**
    手动修改浏览器本地存储中的过期时间戳，将其设置为过去的时间。
    运行指令：`localStorage.setItem('expire_time', '1000')`
2.  **触发路由：**
    点击页面上的任意导航链接。
3.  **预期结果：**
    页面应立即跳转至 `http://localhost:8080/login`，且控制台无报错。

```

---

## 4. 关键注意事项（给AI的指令）

1.  **路径准确性：** 在引用文件时，必须使用项目根目录下的相对路径，例如 \`src/components/Header.vue\`，而不是仅仅写 \`Header.vue\`，除非上下文中文件名唯一。
2.  **保留注释：** 在生成的代码块中，必须保留关键的**英文或中文注释**，特别是对于复杂的逻辑修改，必须在代码内部通过注释解释“为什么这么改”。
3.  **不省略关键行：** 在展示代码片段时，**严禁**随意使用 `...` 省略号跳过关键的闭合括号 `}` 或缩进结构，除非是在显而易见的无关代码段。这能防止用户复制粘贴时破坏代码结构。
4.  **字符隔离执行：** 再次强调，在中文描述段落中，提及任何代码元素（如变量 \`var_name\`）时，务必使用反引号包裹。
