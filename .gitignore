# IDEs and editors
.idea/
.vscode/

# Python
__pycache__/
*.pyc

# Environment variables
.env
backend/.env
.env.local
*.env.local
.env.*
!.env.example

# JWT Secret Key files (安全文件，存储在 backend/credentials 目录，不应提交到版本控制)
.jwt_secret
.jwt_secret.enc
backend/.jwt_secret
backend/.jwt_secret.enc
backend/credentials/.jwt_secret
backend/credentials/.jwt_secret.enc

# ENCRYPTION_KEY files (主密钥文件，存储在 backend/credentials 目录，不应提交到版本控制)
backend/credentials/.encryption_key

# Google Cloud 服务账号密钥
backend/credentials/
*.json
!package.json
!package-lock.json
!tsconfig.json
!manifest.json
!metadata.json

# Databases
backend/test.db
*.db
node_modules

# IDE specific
.idx/
nul

# Hypothesis testing cache (属性测试缓存)
.hypothesis/
backend/.hypothesis/

# Kiro specs directory (排除 .kiro/specs 目录)
.kiro/specs/
